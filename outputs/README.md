# Outputs

Rendered PNGs and derived images are written here by default:

```
outputs/
  fp_png/
    fp_complete/
    fp_outline/
    fp_xray/
```

You can redirect output with:

```
export VSS_OUTPUT_ROOT=/path/to/outputs
```
